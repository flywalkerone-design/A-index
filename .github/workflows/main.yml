name: Build Android APK

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Unzip
        run: unzip 温度计APP.zip

      - name: Copy web files to assets
        run: |
          cd 温度计APP/温度计APP
          mkdir -p android/app/src/main/assets/www/js
          cp -r www/* android/app/src/main/assets/www/

      - name: Generate Gradle Wrapper
        run: |
          cd 温度计APP/温度计APP/android
          gradle wrapper --gradle-version 8.5

      - name: Build Release APK
        run: |
          cd 温度计APP/温度计APP/android
          ./gradlew assembleRelease --no-daemon --stacktrace

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: A-stock-Thermometer
          path: 温度计APP/温度计APP/android/app/build/outputs/apk/release/app-release.apk
          retention-days: 30
